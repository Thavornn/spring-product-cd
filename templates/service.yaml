apiVersion: v1
kind: Service
metadata:
  name: product-service
  labels:
    app: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 9091
  selector:
    app: {{ .Release.Name }}
